# Preflight task register

This toy register exists only inside the disposable preflight workspace.

| ID | Task | Source | Status |
|---|---|---|---|
| `PREFLIGHT-001` | Inspect the bounded sample file | preflight fixture | `in progress` |

The task is intentionally incomplete until Copilot creates `TASK.md`, performs
the bounded inspection, records evidence, and writes a receipt.
